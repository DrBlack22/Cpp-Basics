#include <iostream>
using namespace std;

int multiplyShite(int f, int h){
    int product = f * h;
    return product;
}
int main()
{
    cout << multiplyShite(8, 4);
    return 0;
}
/*  FUNCTIONS THAT USE MULTIPLE PARAMETERS

- RT(Return Type) FN(Function name)
    Parameters inside the parentheses
    Body of function

- While calling the function inside the
main function, we need to pass the values
of the parameters/variables of the same
data type as we had already defined while
defining the parameters of the function.


 */
